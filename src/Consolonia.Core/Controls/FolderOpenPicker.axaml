<dialog:DialogWindow xmlns="https://github.com/avaloniaui"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:storage="clr-namespace:Avalonia.Platform.Storage;assembly=Avalonia.Base"
                     xmlns:controls="clr-namespace:Consolonia.Core.Controls"
                     xmlns:dialog="clr-namespace:Consolonia.Core.Controls.Dialog"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     x:DataType="controls:FolderPickerViewModel"
                     x:Class="Consolonia.Core.Controls.FolderOpenPicker"
                     Icon="📁"
                     Title="{Binding Options.Title}">
    <Grid RowDefinitions="Auto * Auto Auto">
        <Grid ColumnDefinitions="Auto *">
            <TextBlock Text="📁" Margin="0 0 1 0" />
            <TextBox Text="{Binding CurrentFolderPath, Mode=TwoWay}" Grid.Column="1" />
        </Grid>
        <ListBox Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" DoubleTapped="OnDoubleTapped">
            <ListBox.DataTemplates>
                <DataTemplate DataType="{x:Type storage:IStorageFolder}">
                    <Grid ColumnDefinitions="Auto * " Background="Transparent">
                        <TextBlock Text="📁" Margin="0 0 1 0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                    </Grid>
                </DataTemplate>
                <DataTemplate DataType="{x:Type storage:IStorageFile}">
                    <Grid ColumnDefinitions="Auto *"  Background="Transparent">
                        <TextBlock Text="📰" Margin="0 0 1 0"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}" />
                    </Grid>
                </DataTemplate>
            </ListBox.DataTemplates>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <TextBlock Text="Folder:" Margin="0 0 1 0"/>
            <TextBlock Text="{Binding SelectedFolder.Path.LocalPath}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="1">
            <Button Content="OK" Click="OnOK" IsEnabled="{Binding SelectedFolder, Converter={x:Static ObjectConverters.IsNotNull}}"/>
            <Button Content="Cancel" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</dialog:DialogWindow>